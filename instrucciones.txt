ir a la carpeta windows -> SoftwareDistribution

boton derecho "propiedades"

ir a seguridad -> "opciones avanzadas"

seleccionamos "system" -> precionamos "deshabilitar herencia"

en teoria deberia desaparecer todos los permisos

despues asignamos permisos totales a "usuarios"
y "System" asignamos "permitir" a escritura y ademas configuramos los permisos especiales que comprenden
de la siguiente manera

"denegar" debe estar marcado(permisos avanzados) lo siguiente
-> "atravesar carpeta/ejecutar archivo"
-> "leer atributos"
-> "leer atributos extendidos"
-> "permisos de lectura"

"permitir" debe estar marcado (permisos basicos) lo siguiente
-> "escritura"

el objetivo que no quede ningun permiso para el system y adueñarnos todos los permisos

despues agregamos los permisos para el "usuario" y el "system solo que pueda guardar info, pero nunca
poder ejecutarlo" entonces lo que logramos es que microsoft store pueda descargar los archivos de la app
y bloquear las actualizaciones obligatorias.